image_yscale = base_scale + sin(current_time * bounce_speed) * bounce_strength;
image_xscale = base_scale + cos(current_time * bounce_speed) * bounce_strength;

if obj_worldmap_cuphead.y > y{
	depth = obj_worldmap_cuphead.depth + 2	
}	else	{
	depth = obj_worldmap_cuphead.depth - 2	
}
